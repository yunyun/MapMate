class Mapmate {


    /**
     * 构造函数
     *
     * @param ele 元素ID
     * @param options 入参
     */
    constructor(ele, options) {
        this.mapContainer = document.getElementById(ele);
        this.options = options;
        this.maps = {};
    }



}